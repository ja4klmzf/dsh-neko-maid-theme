# 更新日志 · Changelog

## v1.1.7（2026-09-01）

### 🗑 移除自绘右侧导航
- 新版 DSH 已自带右侧会话导航，移除皮肤自绘的「本会话提问导航」（含全部 rail 组件、样式与滚动监听），避免与原生导航重复/冲突
- 猫娘默认右距保持 56px，为 DSH 原生右侧导航让位

## v1.1.6（2026-09-01）

### 🔧 适配 DSH 前端更新（哈希类名变更）
- DSH 升级后会话 UI 的哈希类名全面更换，皮肤选择器同步更新并**硬化**（改用属性/文本选择器，抗后续升级）：
  - 提问导航：改用 `[data-chat-flow-kind="user"]` + `[class*="bubble"]`（不再依赖 flowItem/bubble 哈希类）
  - 回到底部按钮：`EvIC1a_toBottom`（皮肤化 + HUD 避让）
  - 审批面板：`mna1RW_root`（保留"等待审批"文本兜底）
  - 轨迹面板 / 侧栏脚区：新增 `[class*="ledger"]`、`[class*="footArea"]` 等兜底选择器
  - 滚动容器候选：补充 `EvIC1a_scroll` / `TS9iAW_root` / `eGxaPq_scroller`
  - "harness" 徽章：改用 `svg[viewBox="0 0 182 24"]` 属性选择器（不依赖 hHd-Xa_brand 类）
- 兼容项保留：composer（wSkVaW_*/uV2eYG_*）、模型选择器（_7KE1Ra_*）、会话树（YDXeBa_*）经核实未变

## v1.1.5（2026-08-27）

### 💰 多供应商余额与单价
- HUD 按**当前所选模型**自动切换供应商（模型选择器触发按钮上的模型名识别）
  - **DeepSeek**：峰谷价（元）+ 实时余额（api.deepseek.com）
  - **Kimi**：官方美元单价（K3 $3/$15、K2.6/K2.7 $0.95/$4、K2.5 $0.6/$3、K2 $0.6/$2.5，按模型名精确匹配）+ 余额（api.moonshot.cn 可用余额）
  - **GLM（智谱）**：官方美元单价（glm-5.3 $1.4/$4.4、GLM-4.5-Air $0.2/$1.1、GLM-4.6 $0.6/$2.2）+ 余额显示 `--`（无公开接口）
- 非 DeepSeek 供应商单价框显示绿色 `flat` 样式，悬停可见缓存命中价与数据来源

### 🛡 HUD 避让优化
- **模型/推理等级/模式选择弹层打开时 HUD 自动隐藏**，关闭后自动恢复（不再遮挡下拉菜单）
- 修复：DSH 弹层关闭后不卸载、仅淡出（opacity→0）——新增 opacity/pointer-events 判断 + 点击即时重算，HUD 不再被"已关闭的透明菜单"永久隐藏

### 🔧 安装脚本
- install.ps1 改为从 `.credentials.yaml` 生成 `neko-keys.js`（deepseek/kimi/zai 多 Key）；uninstall 同步清理；`.gitignore` 排除本地 Key 文件

## v1.1.4（2026-08-26）

### 🎆 节日彩蛋扩充（4 → 13 个）
- 新增：春节 / 元宵 / 儿童节 / 端午 / 中秋 / 国庆 / 万圣节 / 平安夜
- 农历节日（春节/元宵/端午/中秋）内置 **2026-2040 公历对照表**（查表换算，零依赖；表外年份自动跳过）
- 每个节日 2 条专属台词 + 对应 emoji 雨（春节🧧🐉、端午🐲🥁、中秋🌕🥮、万圣🎃👻…）

### 🖼 README 效果图
- 新增**猫娘表情图鉴**：16 种表情 4×4 九宫格（常态/摸头/挠下巴/摸尾巴/投喂/喝茶/夸奖/亲亲/生气/吃醋/休息/认真/思考/翻书/执笔/终端）

## v1.1.3（2026-08-20）

### ✨ 新增（对齐 DeepSeek 网页版交互）
- **右侧本会话提问导航**（1:1 复刻 chat.deepseek.com 的 scroll-nav，含样式参数）：
  - 收起态：右缘 16px、垂直居中的 **34×300px 磨砂胶囊**（blur 5px、圆角 16px，浅色白 0.8 / 深色黑 0.6）；每条提问显示为 **8×2px 短线**，当前提问线变品牌蓝并放大 1.5 倍
  - 悬停 120ms 展开 **240px 面板**（bg-layer-1 + lv3 阴影 + 1px 边框 + 上下 32px 渐变），提问标题 13px 淡入（激活项品牌蓝 + 500 字重），**移开 400ms 自动回收**（纯悬停交互，无固定状态，与 DeepSeek 一致）
  - 列表 max-height 250px、内边距 15/0/15/24px、右对齐、隐藏滚动条；<768px 自动隐藏
  - 点击提问 → 平滑滚动到对应消息（距顶部 96px）；高亮跟随滚动，最顶端高亮第一条
  - 猫娘默认停靠位右移（右距 56px）为导航让位；导航 z-index 高于猫娘，悬停始终可用（pointermove 兜底）
- **回到最新按钮**：聊天上翻时，对话框上方居中浮现 ▼ 圆形按钮，点击平滑滚回最新消息；已到底时自动隐藏
  - 跟随滚动实时显隐（150ms 节流），并随布局每 3 秒校准位置
  - **峰谷/余额 HUD 与箭头统一锚定输入区上沿**：等待任务栏出现时两者一起顶到任务栏之上，底边对齐、间距统一 12px

### 🐛 修复
- 左上角 "harness" 徽章重做为**反色镂空设计**：药丸底色与文字互为反色——白天深紫药丸 + 近白字，夜间浅紫药丸 + 深紫字，两种模式都清晰可见（此前同色方案导致文字与药丸融为一体不可见）
- 移除皮肤自绘的"回到最新"箭头（与 DSH 原生按钮重复且互相遮挡），改用 **DSH 原生「回到底部」按钮并皮肤化**（粉边圆钮 + 悬停光晕）
- **HUD 不再遮挡原生按钮**：右缘自动让到按钮左侧 10px（偏移量缓存，按钮显隐不引起左右跳动）
- **消除 HUD 漂移**：位置变化加 250ms 平滑过渡；下拉选择器不触发移动；跟随输入框高度变化改为平滑滑动
- **等待审批时不再漂移**：审批面板会接管整个输入区（锚点消失导致 HUD 掉到右下角）——实测真实审批状态后重做：审批期间 HUD **保持"箭头旁"的横向位置不变**，仅平滑上移到面板上方 12px（缓存最近正常态的 right 偏移）；新增 DOM 变更监听，任务栏/审批面板出现消失 200ms 内即时重定位（不再等 3 秒周期）
- 全量代码整理：删除冗余的任务栏检测逻辑与孤儿样式，函数调用链核对一致

## v1.1.2（2026-08-20）

### ⚡ 新增
- 余额框左侧新增**峰谷价格框**：按 DeepSeek V4 分时计价（2026-08-17 起生效）实时显示当前 峰时/谷时 与 输入/输出 单价（元/百万 tokens）
  - 峰时（北京时间 9:00-12:00 / 14:00-18:00）：输入 ¥9 · 输出 ¥27
  - 谷时（其余时间）：输入 ¥4.5 · 输出 ¥13.5
  - 悬停显示缓存命中价（峰 ¥0.30 / 谷 ¥0.15）与"距转峰/转谷"倒计时
- 价格框与余额框合并为 HUD 容器，随布局统一浮动，一起避让等待任务栏与轨迹面板

### 🐛 修复
- 等待任务栏的文本兜底检测限定在输入区（composerSeat）内，避免聊天记录中出现"等待执行"字样时误触发定位

## v1.1.1（2026-08-20）

### 🐛 修复
- 余额显示框在「等待任务栏」出现时自动上移避让，不再遮挡任务预览文字（类名哈希 + 文本双检测，兼容后续版本）
- README 新增效果图（昼夜全貌 + 猫娘特写）

## v1.1.0（2026-08-20）

### 🎙 语音系统重做
- 新增语音问候（刷新页面时一句）与聊天回复语音
- 优先级：Windows 自然语音（微软晓晓，推荐手动安装，离线可用）→ 本机语音代理（Google，兜底）
- 萝莉音参数：音高 2.0 档 + 情绪语速表（开心/害羞/困倦/赌气/亲亲）
- 朗读时"欧尼酱"自动替换为"主人"（气泡文字不变）
- 完全移除本地机械音回退——语音失败时保持安静

### 💬 聊天交互重做
- 移除菜单"聊天"按钮，改为**点击猫娘头顶气泡直接输入**（✈ 发送 / ✕ 取消）
- 输入期间所有后台台词排队等待，不再打断输入
- 长期记忆：记住最近 8 句话 + 自动捕捉"喜欢 XX"偏好

### 🖱 交互与布局
- 互动菜单移到**立绘下方**，不遮挡立绘；鼠标移开延时 650ms 消失
- 猫娘可**拖拽移动**：拖入侧边栏 → 缩小到 1/3 停靠（底部脚区之上）；拖出 → 恢复原位原大小；双击复位
- 侧边栏收起成窄条时猫娘自动隐藏
- 新增**余额显示框**：DeepSeek API 余额，锚定轨迹面板同步浮动，不挡任务栏

### 🧠 智能状态
- 新增 edit（执笔改文件）与 pwsh（终端敲代码）状态表情，与 think/search 同机制
- 状态优先级：吃醋 > 查阅 > 修改 > 终端 > 思考 > 打盹 > 认真 > 常态

### 🏆 养成与彩蛋
- 好感度等级 Lv1~Lv10（十级称号，从"陌生的小猫娘"到"一生的猫娘"）
- 节日彩蛋：元旦 / 情人节 / 七夕 / 圣诞（emoji 雨 + 台词）
- 生日彩蛋（8-17）🎂
- 今日工作小结（夜幕降临时播报）、今日统计（悬停气泡 1 秒）

### ⚙️ 代码优化
- 状态刷新按帧合并（流式期间不再高频查询）
- 猫娘/余额框布局合并为统一 tick
- 修复聊天输入监听器泄漏、气泡互斥等若干问题

---

## v1.0.0（2026-08-19）

初始发布：
- 洛丽塔猫娘桌宠（参考图真人形象 + 蕾丝花环：白天玫瑰藤 / 夜晚珍珠）
- 昼夜双主题：水晶白昼 / 月潮夜晚（专用背景图，自动模式按系统时间切换）
- 互动：投喂 / 摸头 / 挠下巴 / 摸尾巴 / 夸奖 / 休息 / 喝茶 / 主题
- 隐藏成就：连点 10 次解锁"逗她"；投喂+喝茶满 300 次解锁"亲亲"（全屏告白文字雨）
- 工作时长关怀：每 10 分钟请喝茶；连续 6 小时"喝茶"常驻
- 智能表情：think 思考 / search 翻书查阅 / 打字认真 / 回答完成庆祝
- 猫爪光标、猫爪花边（白天粉/夜晚深蓝黑）、花瓣/星空/月光/流星氛围
- 天气联动（下雨提醒+雨丝特效）、吃醋/打盹机制、气泡常驻台词
- DeepSeek 余额显示框、拖拽移动、窗口自适应
